# Design

## Source of truth

- Status: Active
- Last refreshed: 2026-08-05
- Additional evidence reviewed: `docs/adr/0017-catalog-grounded-goal-coverage.md`.
- Primary product surfaces: Blender 3D View Sidebar, Blender 3D View guidance overlay, the headless Orchestrator MCP/HTTP API including host-mediated asynchronous Replan Runs, the opt-in OpenAI composition root, and the host-neutral GuidePlan/GuideProposal protocol that supplies their content.
- Evidence reviewed: `README.md`, `docs/architecture/overview.md`, `docs/architecture/blender-companion.md`, `docs/adr/0010-paginated-revision-history.md`, `docs/adr/0011-cross-target-planning-quality-gate.md`, `docs/adr/0012-provider-neutral-planner-packets.md`, `docs/adr/0013-explicit-planner-provider-boundary.md`, `docs/adr/0014-openai-responses-planner-provider.md`, `docs/adr/0015-typed-provider-local-replanning.md`, `docs/adr/0016-host-mediated-asynchronous-replan-runs.md`, `packages/protocol/src/guide.ts`, `packages/protocol/src/proposal.ts`, `packages/protocol/src/revision.ts`, `packages/protocol/src/replanning-provider.ts`, `packages/protocol/src/companion-replan-run.ts`, `packages/protocol/src/diff.ts`, `packages/protocol/src/history.ts`, `packages/protocol/src/catalog.ts`, `packages/protocol/src/action-arguments.ts`, `packages/protocol/src/planning.ts`, `packages/protocol/src/prompt.ts`, `packages/protocol/src/provider.ts`, `packages/protocol/src/eval.ts`, `packages/planner-provider-sdk/src/index.ts`, `packages/openai-planner-provider/src/index.ts`, `services/orchestrator/src/index.ts`, `services/orchestrator/src/companion-replan-run.ts`, `services/orchestrator/src/planning-prompt.ts`, `services/orchestrator/src/replanning-prompt.ts`, `services/orchestrator/src/replanning-service.ts`, `services/orchestrator/src/local-replan-scope.ts`, `services/orchestrator/src/planning-quality.ts`, `services/orchestrator/src/planner-provider-registry.ts`, `services/orchestrator/src/planner-generation.ts`, `services/orchestrator/src/planner-replan-generation.ts`, `services/openai-runtime/src/config.ts`, `services/openai-runtime/src/index.ts`, `services/orchestrator/src/guide-plan-diff.ts`, `services/orchestrator/src/guide-revision-history.ts`, `services/orchestrator/src/eval-export.ts`, `packages/persistence/src/index.ts`, `adapters/blender/catalog/v1/action-catalog.json`, `adapters/blender/extension/operating_line/application/companion.py`, `adapters/blender/extension/operating_line/application/provider_handoff.py`, `adapters/blender/extension/operating_line/application/revision_review.py`, `adapters/blender/extension/operating_line/application/session.py`, `adapters/blender/extension/operating_line/presentation/panel.py`, `adapters/blender/extension/operating_line/presentation/operators.py`, `adapters/blender/extension/operating_line/presentation/revision_workspace.py`, `adapters/blender/extension/operating_line/infrastructure/overlay.py`, `adapters/blender/extension/operating_line/resources/snowman.plan.json`, `protocol/fixtures/v1/planning/robot-preview.benchmark.json`, `tests/integration/blender/test_planning_benchmark.py`, `tests/e2e/blender/capture_overlay.py`, and `docs/assets/blender-guidance.png`.
- Product boundary: the shipped baseline is a deterministic 15-step Blender snowman vertical slice, including bounded rigid armature binding and pose keyframes, plus model-neutral ActionCatalog/PlanningContext, versioned initial-planning and local-replanning packets delivered through MCP Prompt/Tool or HTTP, an explicitly injected in-process Planner Provider contract with optional typed `replan()`, an optional OpenAI Responses provider and separate opt-in composition root, a five-phase planning profile, deterministic structural quality and referenced-subtree locality gates, a real non-snowman robot benchmark, human-approved GuideProposal, node references, immutable linear revision threads, deterministic Plan diffs, paginated structured revision history, and versioned raw Eval/replay export. Blender presents revision work in one collapsible native Revision Workspace: references remain structured and separate from the message body, each reference can be removed independently, an attempted cross-base reference never silently discards a draft, and the relevant thread, provider handoff, proposal, diff, and decision remain together. A Blender user may reference nodes in an active or proposed tree, queue a revision request, then either leave it for Codex/Claude through MCP or explicitly select one registered Provider, inspect its data-handling disclosure, and authorize one asynchronous Replan Run. The Run composes the existing provider generation and canonical Proposal submission only after that host confirmation; the Proposal still requires a separate Accept/Reject and cannot execute itself. Provider generation continues to return an untrusted draft plus deterministic quality and locality evidence with `proposalCreated: false`. The default standalone does not load a provider or credentials. OperatingLine does not automatically select or invoke a provider, model, credentials, or goal phases; stream a chat response; preserve model reasoning; infer a semantic or aesthetic quality score; or automatically sanitize exported evidence. Parameter-form editing, explicit branching/merging, arbitrary-goal semantic validation, scoring/training governance, deform/weight-painted rigs, and a second software host remain later milestones.

## Catalog-grounded goal coverage

- Blender ActionCatalog `1.3.0` adds seven adapter-owned `semanticCapabilities` over the existing ten allowlisted actions. Capability-aware Planning and Replanning Packets use format `1.1.0`; historical catalogs remain replayable with packet `1.0.0`. The corresponding planning-quality baselines are `1.1.0` and `1.0.0`.
- A provider draft must declare a trace from each concrete requirement to one or more exact catalog capabilities and then to executable leaf step IDs whose actions implement those capabilities. Local replanning limits those step IDs to the normalized referenced subtrees.
- Missing coverage, unknown capabilities, nonexistent or actionless steps, action/capability mismatches, and out-of-scope replan mappings produce deterministic errors. Provider generation returns `needs_revision` with `proposalCreated: false`; the trace and report enter quality events and raw Eval/replay evidence.
- This trace is provider-authored audit evidence, not OperatingLine-owned semantic truth. It has no scalar score, does not prove requirement extraction or parameter semantics, does not select or invoke a provider, does not mutate a scene, and does not bypass Proposal review, in-host acceptance, or explicit `Start`/`Next` execution. See [ADR 0017](docs/adr/0017-catalog-grounded-goal-coverage.md).

## Brand

- Personality: calm, precise, teachable, and visibly accountable. The UI should feel like an industrial operation guide rather than a decorative assistant.
- Trust signals: every visible state maps to an actual plan step; proposed plans are visibly distinct from accepted plans; no proposal can execute before an explicit in-host acceptance; object/world anchors point to resolved Blender data; unresolved native UI targets are described as semantic paths instead of receiving invented coordinates.
- Avoid: black primary guidance lines, color-only meaning, fake claims that deterministic data-API actions clicked Blender menus, floating desktop overlays, and visual noise that obscures the model.

## Product goals

- Goals: show what has completed, what Back will undo, what Next will execute, and what remains locked; keep the task hierarchy readable; give every model client the same versioned planning or replanning packet, exact host context, strict response contract, and evaluate-before-propose workflow; let users inspect and explicitly accept or reject an AI-authored plan without scene mutation; let a user add stable references from active or proposed nodes to one collapsible Revision Workspace, remove one reference without rebuilding the draft, and write a message that is visibly separate from those references; reject an attempted cross-base reference while preserving the current draft; preserve exact linear turn lineage; show Plan/node/field/simple-parameter changes before acceptance; return every requested change as a reviewable full Plan revision; keep each thread's request, provider authorization/run state, proposal, diff, and decision in one host-native work area; make Provider choice explicit, disclose local/remote transmission and possible charges before every invocation, keep long-running generation off Blender's short polling path, let users page backward through exact revision history, and keep Revision Workspace available even when viewport guidance and task-tree guidance are hidden.
- Non-goals: a vendor-backed default runtime, automatic provider/model selection or invocation from Blender, an arbitrary-goal task-decomposition engine, core-owned credentials, process-level isolation for in-process plugins, streaming assistant chat or preservation of private model reasoning, editable parameter forms, revision branching/merging, in-place mutation of an accepted/proposed Plan, applying a partial JSON patch directly in Blender, automatic Eval scoring or data sanitization, arbitrary built-in UI-element detection, arbitrary MCP execution, native Blender Undo integration, deform rigs/weight painting or arbitrary animation editing, and cross-host parity in this milestone.
- Success signals: MCP Prompt, MCP Tool, and HTTP reuse the same deterministic Planner Packet builder for the same context; Prompt presents the packet's rendered model text while Tool/HTTP return the complete packet; the packet requires one strict GuideProposal draft and instructs `planning.evaluate -> guide.propose`; prompt generation emits a replayable `planning.prompt.generated` event without invoking a model; an explicitly injected provider receives that exact packet and an `AbortSignal`, public listing exposes no credentials, provider selection remains explicit, and standalone remains provider-free; the optional OpenAI provider requires an explicit model and credential, uses the official Responses SDK with `store: false`, a 32,768-output-token cap, `maxRetries: 0`, cooperative cancellation and JSON Object mode, and is loaded only by the separate `pnpm dev:openai` composition root; output is reparsed as an untrusted strict draft, checked against immutable packet identity, nested ActionCatalog arguments and the same deterministic planning-quality gate, then returned with `proposalCreated: false`; failed or interrupted request IDs require a new explicit ID for retry; generation requested/completed/failed evidence is replayable without persisting raw provider errors or private reasoning; the same deterministic gate rejects invalid phase trees or resource flow before Proposal submission; evaluation emits `planning.quality.evaluated` without a scalar score; snowman and robot references pass while the robot intentionally omits animation; a referenced revision request contains the exact base Plan, stable node IDs plus display numbers, user message, thread ID, turn and parent; exact retries are idempotent; a continued turn must use an accepted parent Proposal in the same instance as its exact base and cannot branch silently; an MCP client can list pending requests or build the exact Replanning Prompt Packet, explicitly invoke a provider's typed `replan()`, receive a complete candidate plus deterministic diff/locality/quality with `proposalCreated: false`, and submit only that canonical generated draft as a request-linked Proposal in a separate call; another generation cannot claim its provenance; Blender's Revision Workspace keeps structured references separate from the message, supports per-reference removal, retains but blocks a draft after a base mismatch, keeps thread/provider-run/proposal/diff/decision together, and remains available when Guidance is hidden; after runtime acknowledgement it lists Provider descriptors without credentials, leaves selection empty, explains transmission and possible charges, and requires a fresh host confirmation for every Run or retry; Run start returns promptly while Orchestrator performs generate and exact canonical propose asynchronously; one instance cannot hide concurrent runs or Proposals; interrupted runs never silently invoke the Provider again; request submission, history retrieval, Replan Run states, proposal preview, and provider generation do not mutate the scene or active session; the replacement proposal still requires explicit in-host acceptance; replay export preserves target, prompt contract, generated draft, exact catalog, quality/locality findings, proposal, diff, decision, observation and rollback with a stable cursor and deterministic content hash; Blender 4.5 LTS and 5.1 tests pass.

## Personas and jobs

- Primary personas: Blender beginners supervising AI-assisted work; experienced Blender users auditing or correcting a generated sequence; adapter developers implementing another host companion.
- User jobs: understand the overall task tree; review the hierarchy and executable leaves proposed by an AI; reference one or more nodes without copying fragile text; explain the desired change in host context; know which Plan revision and revision turn are being discussed; review earlier requests, returned revisions, diffs, and decisions; inspect exactly which Plan fields, nodes and simple parameters changed; accept or reject the resulting proposal inside the host; predict the next mutation before executing it; identify the exact step Back will compensate; temporarily remove guidance without losing task state.
- Key contexts of use: Blender's 3D View with a narrow Sidebar, light or dark Blender themes, rendered or solid shading, and scenes containing user-owned objects that must not be mistaken for OperatingLine resources.

## Information architecture

- Primary navigation: Blender's `N` Sidebar contains one `OperatingLine` tab. It is the stable control surface; the viewport overlay is contextual guidance, not navigation.
- Core routes/screens: connection status; collapsible Revision Workspace containing draft context, structured references, message, Provider disclosure and Run status, thread history, pending proposal, diff, and decision; safety option; walkthrough controls; guidance visibility; current Back/Next status; proposal tree; and accepted task tree.
- Content hierarchy: connection first; Revision Workspace second, with current draft and acknowledged request before Provider choice/disclosure/Run state, followed by the related thread/proposal/diff/decision log; safety and accepted-plan Start/Back/Next controls third; guidance visibility fourth; current status fifth; proposed and accepted trees after the workspace that receives their references; detailed protocol/diagnostic text last. Collapsing the workspace hides its detail without deleting draft, Run, proposal, or review state.

## Design principles

- State before decoration: every color, number, icon, and line represents a deterministic state derived from `active_index`.
- Review before mutation: receiving and validating a proposal only creates an in-memory preview. Accept is the sole transition that may replace an idle active session; Reject only records the decision and clears the preview.
- Reference before revision: node numbers are readable locators, but immutable requests bind the complete base Plan, stable node IDs and a linear thread turn. A model response is a new complete proposal plus a derived review diff, never an in-place tree mutation or executable patch.
- Preserve before rebasing: selecting a node from a different active/proposed Plan base must not silently delete or rebind the user's references or message. Reject that selection and require a deliberate Clear Draft before switching bases.
- Disclose before transmission: a Provider descriptor informs but does not authorize. Leave Provider selection empty, show execution location, data handling, payload scope and possible-charge language, then require a fresh native confirmation for each invocation or retry.
- Honest anchors: resolve model/world targets when possible, show a breadcrumb for `operator.menuPath`, and explicitly label unavailable UI targets. Never draw a plausible but false line.
- Progressive focus: show at most the most relevant completed, Back, and Next steps in the viewport while retaining the complete hierarchy in the Sidebar.
- Host-native control: use Blender Panel, Operator, draw handler, `gpu`, and `blf`; do not depend on a transparent Electron window over Blender.
- Tradeoffs: Blender `UILayout` does not expose arbitrary per-button background colors or stable rectangles for every built-in widget. Use native alert state, colored node sockets, icons, text, and viewport drawing; reserve custom gizmos for a later interactive-anchor milestone.

## Visual language

- Color: `surface #101820`, `halo #071018`, `text #F5F7FA`, `completed #2F9BFF`, `current #FFC857`, `next #2DD881`, `back #FF5C6C`, and `locked #7B8494`. Black/dark halo may outline a colored line but is never the primary line.
- Typography: inherit Blender UI fonts in the Sidebar; use Blender's `blf` default font in the viewport. Global executable ordinals use two digits (`01`–`15`); tree nodes retain hierarchy numbers (`1.2.1`).
- Spacing/layout rhythm: use Blender-native row/box spacing. The viewport card keeps at least 16 px from an edge, uses a 32 px circular step marker, and prioritizes one Back and one Next item.
- Shape/radius/elevation: native Sidebar surfaces; flat, high-contrast viewport cards; circular ordinal badges with a dark outer ring and colored inner fill.
- Motion: the scene may contain explicit plan-authored keyframes, but the guidance UI has no required motion. Any later UI transition must respect reduced-motion settings and must not delay execution feedback.
- Imagery/iconography: `✓`/check for completed, `↶`/Back for the reversible active step, `▶`/Next for the next executable step, and an open circle for locked. Text labels duplicate all icon meaning.

## Components

- Existing components to reuse: Blender Sidebar `Panel`, `UILayout` rows/boxes/operators, task-tree branch operator, `SpaceView3D.draw_handler_add`, `gpu` primitives, and `blf` text.
- New/changed components: versioned planning-phase, initial Planner Packet, Replanning Prompt Packet, Proposal/replan-draft, Planner Provider descriptor/request/result/error, asynchronous host-mediated Replan Run and strict quality/locality-report contracts; provider SDK interface, explicit registry and shared bounded invocation coordinator; optional official-SDK OpenAI Responses provider and a separate opt-in runtime composition root; deterministic prompt-packet builders and planning-quality/locality evaluators; shared guidance-state derivation; shared visual tokens; state-aware tree rows; node `Ref` operators for active and proposed trees; one collapsible native Revision Workspace with base-plan/next-turn label, structured reference rows, per-reference Remove, a separate message field, explicit Clear and Send controls, Provider selection/disclosure/confirmation, Run status, same-area thread/proposal/diff/decision log, and Load Older Turns; Back/Next status controls; numbered viewport badges; haloed colored guide lines and arrows; semantic menu-path fallback; unified Show/Hide Guidance behavior that never hides Revision Workspace; read-only proposal tree; Accept Plan and Reject Plan operators.
- Variants and states: accepted walkthrough steps use `completed`, `back`, `next`, and `locked`. Revision Workspace uses `expanded` and `collapsed` without changing its contents or availability. Proposal lifecycle uses `available`, `accepted`, and `rejected`; request-linked proposals additionally use `diff_available`. Revision requests use `empty`, `draft`, `base_mismatch`, `queued`, `acknowledged`, and `error`, with `new_thread` or `continue_thread` context; queued/acknowledged requests never imply a model has replied. Provider handoff uses `provider_unselected`, `provider_selected`, `authorization_required`, `queued`, `generating`, `proposal_created`, `needs_revision`, `failed`, and `interrupted`; retries always return to `authorization_required` with a new generation ID. Revision-history turns use `awaiting_proposal`, `awaiting_decision`, `accepted`, and `rejected`, plus loading/complete pagination copy. Synchronous Blender actions do not pretend to display a state that cannot be rendered during execution.
- Token/component ownership: host-neutral step/anchor, planning-phase/intent/planning-packet/replanning-packet/quality/locality-report, provider descriptor/request/result/error, Replan Run request/status, proposal/decision, node-reference, revision-thread, Plan-diff, revision-request, revision-history page, and Eval bundle semantics stay in `packages/protocol`; the minimal provider interface, including optional `replan()`, stays in `packages/planner-provider-sdk`; the official SDK adapter, OpenAI request policy and sanitized response extraction stay in `packages/openai-planner-provider`; explicit environment configuration and OpenAI-only composition stay in `services/openai-runtime`; adapter-specific phase-to-action assignments stay in each versioned ActionCatalog; proposal/revision-request persistence, Provider generation and Run lifecycle provenance, thread uniqueness, idempotency, and stable event sequencing stay in `packages/persistence`; deterministic prompt rendering, provider registration and shared bounded invocation, strict output/identity/catalog validation, quality/locality evaluation, thread continuity and accepted-parent validation, canonical generated-draft matching, asynchronous Run coordination, deterministic diff computation, history joins/pagination, Eval relation filtering, summaries, and content hashing stay in the Orchestrator; model client, credentials, network/cost policy and `AbortSignal` cooperation stay inside each injected provider plugin; MCP/HTTP clients may continue the separate generate/propose path, while a host-confirmed Replan Run composes those existing authorities without changing them; deterministic accepted-session, provider disclosure/run presentation, proposal preview, diff/history validation, page merging, and local Revision Workspace draft lineage stay in Blender application code; Blender RGBA and drawing constants stay in Blender presentation/infrastructure code.

## Accessibility

- Target standard: WCAG 2.2 AA contrast intent where Blender's host rendering allows it; no meaning depends on red/green alone.
- Keyboard/focus behavior: retain native Blender operators so keyboard search and focus behavior remain host-managed. The overlay itself is informational and non-interactive.
- Contrast/readability: colored lines receive a 10 px dark halo below a 4 px state-colored stroke; markers combine color, ordinal, and symbol; text uses high-contrast light foreground on the dark card.
- Screen-reader semantics: Blender's Python UI API has limited assistive-semantic controls. Operator labels and descriptions must remain explicit (`Back: ...`, `Next: ...`) and avoid icon-only controls.
- Reduced motion and sensory considerations: no flashing or required guidance animation; status changes are immediate and persistent. Scene animation is a user-visible output and can be inspected as discrete keyframes.

## Responsive behavior

- Supported breakpoints/devices: Blender desktop UI at supported 4.5 LTS and 5.1 versions; mouse/keyboard workflows; no mobile or touch target in this milestone.
- Layout adaptations: compact control labels follow Blender-native sizing in a narrow Sidebar; Revision Workspace can collapse to a one-row summary without losing state; structured references wrap independently from the message and each keeps a reachable Remove control; exact revision messages wrap inside their log entries instead of being truncated; viewport anchors clamp to its visible region; the card avoids the Sidebar edge and limits contextual items instead of rendering all 15 lines.
- Touch/hover differences: none for this milestone. Operator descriptions provide native hover tooltips.

## Interaction states

- Loading: Companion connection uses existing status text; deterministic local actions are synchronous and do not show a fake loading animation.
- Empty: before Start or after a full rollback, step `01` is green Next and Back is disabled.
- Proposal available: show its title, plan ID/revision, target adapter, optional revision thread, deterministic `+ / - / ~ / moved` summary, changed nodes/fields/simple parameters, read-only hierarchy, and explicit Accept/Reject controls. Do not draw viewport target lines for unaccepted steps because their object anchors may not exist yet.
- Proposal blocked: when the accepted session owns receipts, keep the proposal visible, disable Accept, disable Start/Next, preserve Back, and explain that the user must return the current walkthrough to its start before accepting.
- Proposal accepted: atomically replace only an idle accepted session, clear the preview, record the decision, and emit the existing `plan_loaded` state report before any action can run.
- Proposal rejected: clear the preview, record the decision, preserve the active session, receipts, scene, overlay state, and current step.
- Revision Workspace collapsed: keep a compact industrial-log summary with draft/reference count or latest thread state. Hide only workspace detail; do not discard draft, proposal, history, diff, or decision state, and do not couple this toggle to Guidance visibility.
- Revision request empty: show the current base Plan and `Select Ref on a task node`; Send is disabled.
- Revision request draft: show up to eight structured `@number title` reference rows above, not inside, the user-editable message field. Each row has an individual Remove action. Show the new/continued thread and next turn. `Ref` targets the proposed tree when one is pending, otherwise the active tree.
- Revision request base mismatch: if the user selects a node from the other visible active/proposed base, retain the message and existing references, report that the draft is already bound, and require Clear Draft before switching. Lifecycle actions that deliberately accept, reject, replace, or disconnect a Plan retain their existing safety cleanup/rebind rules.
- Revision request queued: clear the draft only after it enters the authenticated transport queue; show the request ID and state that no scene change occurred.
- Revision request acknowledged: state that an MCP planner may now read it; do not imply a response time or completion.
- Provider unselected/unavailable: after request acknowledgement, list only runtime descriptors and select none by default. Keep the external MCP path valid. Unavailable Providers remain visible with their reason but cannot start a Run.
- Provider selected: show exact display name/version/description and distinguish `local + no transmission` from `remote + provider-managed transmission`. For remote execution, state that the revision message, complete base Plan and references, ActionCatalog, and latest Companion state are sent; state that Provider use may incur charges and OperatingLine cannot estimate them.
- Provider authorization: `Confirm Provider Run` opens a native confirmation for this invocation only; a retry is labeled `Confirm New Provider Run`. Confirmation authorizes the selected Provider call and, only if its canonical result is ready, creation of one review Proposal. Changing Provider/request, disconnecting, reaching a terminal state, or retrying invalidates the confirmation; no persistent consent toggle exists.
- Replan Run queued/generating: poll a short-lived status resource while keeping Companion guide, report, history, accepted-plan, and walkthrough controls responsive. To preserve the identity of a potentially billable invocation, disable a second revision submission, Provider refresh, and any concurrently delivered Proposal decision until the Run becomes terminal; draft edits remain local. Ordinary Plan or Proposal delivery and an exact duplicate acknowledgement must not clear or impersonate the active Run. These states never imply a scene or active-session change and do not expose private model reasoning. `proposal_created` hands review back to the existing Proposal delivery and Accept/Reject lifecycle.
- Replan Run needs revision/failed: show deterministic findings or a sanitized stable error plus retry guidance. Retry keeps the acknowledged revision request, creates a new generation UUID, and requires a new confirmation; it never silently repeats a possibly paid call.
- Replan proposal available: display source request ID, thread/turn, exact diff, and Accept/Reject decision in the same Revision Workspace as the originating request; it remains non-executable until Accept. Accepting it records lineage for the next active-plan request.
- Revision history available: in the same Revision Workspace, show the exact user message and structured references, returned plan revision/diff summary, and derived decision state for each loaded turn. Collapse to the latest three by default, expand all loaded turns on request, and expose Load Older Turns only when the protocol page has a continuation cursor. History is an industrial operation log, is read-only, and never implies live chat or captured model reasoning.
- Error: retain the existing explicit Operator/Companion error report; do not advance visual state when an action or rollback fails.
- Success: steps before `active_index` are blue completed; `active_index` is red Back; `active_index + 1` is green Next.
- Disabled: future steps are gray locked; Back is disabled before any step; Next is disabled after the last step.
- Offline/slow network, if applicable: offline mode continues with the last accepted or bundled plan; network state must not block accepted-plan guidance controls when no proposal decision is pending. A locally staged proposal remains a connected-session review item and is cleared on explicit disconnect.

## Content voice

- Tone: short, instructional, factual, and explicit about consequences.
- Terminology: `Revision Workspace`, `Plan proposal`, `Revision request`, `Provider`, `Confirm Provider Run`, `Confirm New Provider Run`, `Replan Run`, `Reference`, `Remove reference`, `Send request`, `Accept Plan`, `Reject Plan`, `Active plan`, `Guidance`, `Back`, `Next`, `Completed`, `Locked`, `UI target unavailable`, `Menu path`, and protocol step numbers. Call the workspace an operation log, not live `Chat`; avoid `Publish` for an unapproved AI proposal and `Undo` for OperatingLine compensation.
- Microcopy rules: Proposal, request and Run text says that no scene change has occurred; queued request means stored for a planner, while queued Run means authorized background work; descriptor text never claims consent, exact pricing, retention policy or sandboxing; collapse/expand copy never implies deletion; a base mismatch names both revisions and the explicit action required; Accept names the plan that will become active; Next describes the mutation that will occur; Back names the mutation that will be compensated; structured reference rows display `@number title` while payloads retain stable node IDs, and the free-form message never duplicates hidden reference syntax; English UI strings remain concise until repository-wide localization is introduced.

## Implementation constraints

- Framework/styling system: Blender Python Extension APIs only for the in-host surface. Electron is not part of the Blender visual layer.
- Design-token constraints: Sidebar colors must use APIs supported by both Blender versions; viewport colors use centralized RGBA tokens. Do not ship per-theme pixel coordinates.
- Performance constraints: draw callbacks perform no network calls or scene mutation, derive only a small relevant-step window, and avoid persistent per-frame allocations where practical.
- Compatibility constraints: use public Blender 4.5/5.1 APIs; keep host-neutral protocol anchors semantic; do not require a Blender Core PR.
- Proposal/request constraints: proposal delivery shares the existing authenticated loopback poll; proposal, decision, revision-thread, Plan-diff, revision-request, history, and Replan Run payloads are strict, versioned, persisted, and idempotent per companion instance. A request carries the complete immutable base Plan so bundled/offline-origin plans can be replanned without an undocumented server cache. A continued turn must point to the current head, exactly reuse its linked Proposal Plan, and prove that Proposal was accepted in the same instance; current persistence rejects duplicate turns and parents. History is joined from normalized facts, newest-page-first with `beforeTurn`, not duplicated into a mutable chat table. A Run POST persists authorization and returns promptly; its Orchestrator background task reuses provider generation and canonical propose, while status polling never carries raw output or reasoning. At most one nonterminal Run may belong to an adapter instance, and every targeted Proposal writer atomically competes for one unresolved Proposal slot; a concurrent external winner makes the Run fail safely instead of hiding another Proposal. Blender retains the exact acknowledged revision and generation identity until that Run is terminal: only an exact pending acknowledgement may bind initially, exact repeats are no-ops, active-run workspace operations cannot clear it, and a non-active Plan replacement invalidates obsolete request context so a late acknowledgement cannot revive it. Proposal delivery and Run status are independently ordered, so Blender keeps a bounded candidate set keyed by `(revisionRequestId, proposalId)`, binds only the exact Provider result, never converts capacity pressure into a user Reject decision, and rechecks `planDiff.basePlan` against the active Session before Accept. A legacy request-linked Proposal without a verifiable diff base remains read-only and rejectable, but Accept fails closed with an explicit reason. Restart reconciliation uses durable completed/proposed evidence without invoking the Provider again; only uncertain work becomes interrupted and needs a new generation ID. Revision Workspace state is Blender-local presentation/application state: collapse and Guidance visibility do not erase it, references remain structured records separate from the message, and an attempted cross-base selection is rejected without silently clearing or rebinding the draft. The background thread handles only short HTTP operations; reference selection/removal, request/thread construction, provider disclosure/run-state validation, plan/diff/history validation, history page merging, preview-session construction, acceptance, rejection, and session replacement occur on Blender's main thread.
- Provider constraints: providers are optional in-process dependencies supplied only through `startRuntime`; the standalone entry point performs no module discovery and remains provider-free. Public descriptors contain no secrets and must disclose execution location, data transmission and provider-owned credential management. Every initial or replan generation names one provider and UUID request ID explicitly; there is no default selection or automatic invocation. `generate()` receives the strict initial Planner Packet, optional `replan()` receives the strict Replanning Prompt Packet, and both receive a cooperative `AbortSignal`; the core reparses the unknown return, enforces a bounded size, immutable packet identity, recursive ActionCatalog argument constraints and deterministic quality evaluation, additionally enforces referenced-subtree locality for replans, and always returns `proposalCreated: false`. The OpenAI plugin is remote/provider-managed and requires an explicit API key plus model. Its official Responses SDK request fixes `store: false`, `stream: false`, a 32,768-output-token cap and `maxRetries: 0`, forwards the signal, and uses JSON Object mode because dynamic action-argument and observation-parameter records do not fit the vendor strict Structured Outputs subset; JSON mode does not replace core validation. `services/openai-runtime` is a separate opt-in composition root exposed as `pnpm dev:openai`; it is not imported by `pnpm dev`. In-process injection is not a security sandbox. Successful generated drafts enter raw Eval evidence and may be sensitive. Completed exact retries replay evidence; failed, timed-out or interrupted request IDs require a new explicit ID so the runtime never silently repeats a paid or side-effecting provider call.
- Eval constraints: export is an authenticated, paginated Orchestrator surface rather than Blender UI state. It scopes instance-specific decisions and reports, includes generated initial/replanning packets, provider-generated strict drafts, exact referenced catalogs, raw deterministic planning-quality/locality findings, and explicit replan-to-proposal provenance; it marks raw content as unredacted and never upgrades prompt generation, generation status, structure/locality findings, or observation telemetry into a semantic quality score. Random export envelope metadata is excluded from the deterministic content digest.
- Test/screenshot expectations: targeted protocol/persistence/integration coverage for identical deterministic planning/replanning packets across entry points, strict proposal/replan-draft/provider/Run wire schemas, provider registration/listing, explicit initial/replan generation, referenced-subtree locality, shared concurrency, idempotency/restart replay, timeout/stop cancellation, sanitized errors, no automatic Proposal creation outside a host-authorized Run, canonical generated-draft provenance, historical-catalog refusal, recursive action-argument validation, phase-profile integrity, deterministic planning findings, OpenAI provider request policy/AbortSignal/response handling, opt-in runtime configuration, request idempotency, node/base-plan validation, accepted-parent thread continuity/no silent branch, deterministic diff ordering/content, pending MCP listing, paginated history, and replan-to-proposal linkage; Run coverage proving strict authorization, fast `202`, ready/needs-revision/failure/restart/retry/concurrency behavior, and no raw credentials/errors/reasoning; Blender coverage proving ACK gating, empty default Provider selection, unavailable-provider refusal, disclosure/confirmation reset, short Run polling, workspace collapse, Guidance hiding, per-reference removal, retained cross-base-selection drafts, node reference, multi-turn request submission, diff preview, and history retrieval do not execute actions or replace sessions; Blender 4.5/5.1 regressions for the six-action robot benchmark, armature binding, keyframe creation, selected-frame rendering, failure compensation, and full rollback; real GUI captures for provider-free, remote disclosure, Run progress/failure and proposal-review states in addition to existing visual states.

## Open questions

- [ ] Should a later interactive milestone use a `GizmoGroup` for clickable OperatingLine-owned viewport controls? / Product / Changes interaction architecture, not this vertical slice.
- [ ] Which second open-source host will validate the protocol's cross-host semantics? / Product and adapter research / Required before claiming general host support.
- [ ] Which human-labeled goals and acceptance rubric should validate semantic completeness beyond the deterministic structure gate? / Product and evaluation research / Required before claiming arbitrary-goal planning quality.
- [x] Which explicit provider/plugin contract should optionally execute Planner Packets without putting credentials or data-transfer policy in the core Orchestrator? / Resolved by explicit in-process injection, provider-owned credentials and transmission, strict untrusted-output validation, no auto-propose, and provider-free standalone; see ADR 0013. A future out-of-process plugin may strengthen isolation without changing the public generation contract.
- [x] How should the first vendor adapter call OpenAI without changing the provider-neutral default? / Resolved by an optional official-SDK Responses provider, explicit model/key, JSON Object mode followed by core strict validation, disabled SDK retries, cooperative cancellation, and a separate opt-in composition root; see ADR 0014.
- [x] Which versioned action-catalog schema should planners query before generating plans for a host? / Resolved by adapter-owned ActionCatalog plus `operatingline.planning.context`; see ADR 0005.
- [x] How should a node reference such as `1.1.5` become an immutable patch request and a new proposal revision? / Stable node ID + display number, complete immutable base Plan, persisted RevisionRequest, and a linked newer full GuideProposal; no in-place patch.
- [x] How should Blender explicitly authorize a long Provider replan without freezing Companion polling or bypassing Proposal review? / Resolved by a host-mediated asynchronous Replan Run with per-call disclosure confirmation, persisted lifecycle, short status polling, canonical generate-to-propose composition, new-ID retry, and unchanged Accept/Reject execution boundary; see ADR 0016.
- [ ] Should localization begin with Chinese/English UI strings or wait for the second host? / Product / Affects string ownership and screenshot baselines.
- [ ] Which parameter fields can safely become host-native editable controls while still producing a complete new Proposal revision? / Product and protocol / Required before parameter-form editing.
- [ ] Can a future Blender-version-specific locator safely resolve selected built-in menu items without fixed pixels? / Blender adapter research / Required before promising native-menu arrows.
