---
'@operatingline/protocol': minor
'@operatingline/orchestrator': minor
'@operatingline/cli-runtime': patch
'@operatingline/openai-runtime': patch
---

Add explicit, quota-aware import of one OAuth-authorized YouTube caption track through the official
Data API, producing a provenance-bound Procedure authoring packet without persisting credentials or
raw caption documents.
