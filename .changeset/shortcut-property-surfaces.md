---
'@operatingline/protocol': minor
'@operatingline/persistence': minor
'@operatingline/orchestrator': minor
---

Add versioned shortcut operator-property surface operations, exact surface-context indexing and search,
and fail-closed Blender catalog parsing while preserving all legacy shortcut materialization outputs.
