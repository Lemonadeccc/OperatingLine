---
'@operatingline/protocol': minor
'@operatingline/orchestrator': minor
---

Add a fail-closed, server-authorized YouTube tutorial media analysis contract and runtime with a
fixed local seven-stage pipeline, content-addressed evidence, deterministic segmentation, async
MCP/HTTP jobs, and explicit full restart from download without partial-stage reuse.
