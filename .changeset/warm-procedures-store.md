---
'@operatingline/protocol': minor
'@operatingline/persistence': minor
'@operatingline/orchestrator': minor
---

Add an immutable, integrity-addressed ProcedureTree library with validated storage, exact/latest reads, stable paginated summaries, conflict-safe revisions, durable audit events, and MCP/HTTP access that never proposes or executes host work.
