---
'@operatingline/protocol': minor
'@operatingline/orchestrator': minor
'@operatingline/blender-action-catalog': minor
---

Freeze InteractionCatalog 1.38.0 and publish 1.39.0 with an exact, accepted-replay native
Subdivision Surface shortcut proof for Blender 4.5.3 and 5.1.1. The new independent Companion path
binds four `Window.event_simulate` operation receipts, a strong Observation, and native Undo/Redo
history while explicitly excluding OS HID input, managed Action execution, and authority for every
other shortcut or target profile. The factory Cube boundary includes canonical transform, mesh
connectivity, and Subdivision-affecting attributes; a durable terminal outbox and exact marker
reconciliation recover an ACK-uncertain result after a same-document Controller, transport, or
Runtime restart without replaying Blender input. Native Undo/Redo transitions are persisted before
publish and atomically reconcile only the strict suffix after the server's current result. Companion
routing identity lives outside `.blend` data. Terminal challenges rotate with the newest replacement
lease, while the native-input driver revalidates authority before every timer turn and is stopped
before disconnect, unregister, or document replacement. File loads, copied files, and OS-process
restarts therefore do not inherit an old target lease.
