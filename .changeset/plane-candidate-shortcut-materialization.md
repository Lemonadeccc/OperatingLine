---
'@operatingline/blender-action-catalog': minor
'@operatingline/orchestrator': minor
---

Materialize the catalog-declared Plane candidate shortcut as six exact ordered operations with closed
location, size-to-scale, and object-name bindings while preserving explicit MCP unavailability. This
reuses the existing materialization schema and adds no protocol or schema behavior.
